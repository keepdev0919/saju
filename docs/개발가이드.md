# 🛠️ 사주풀이 플랫폼 - 개발 구현 가이드

> 각 Phase별 구현 방법과 코드 예시를 담은 가이드 문서입니다.
> 실제 구현 전에 전체적인 감을 잡기 위한 문서입니다.

---

## 📚 목차

1. [Phase 2: 백엔드 연동](#phase-2-백엔드-연동)
   - Express 서버 구축
   - MySQL DB 설계
   - 사주 API 연동
   - 포트원 결제 연동
2. [Phase 3: 알림 및 PDF](#phase-3-알림-및-pdf)
   - 카카오 알림톡 연동
   - PDF 생성 기능
3. [Phase 4: 관리자 페이지](#phase-4-관리자-페이지)
4. [전체 시스템 흐름도](#전체-시스템-흐름도)

---

## Phase 2: 백엔드 연동

### 1. Express 서버 구축

#### 📁 프로젝트 구조 (백엔드)

```
backend/
├── package.json
├── src/
│   ├── index.js          # 서버 진입점
│   ├── config/
│   │   └── database.js   # DB 연결 설정
│   ├── routes/
│   │   ├── payment.js    # 결제 관련 API
│   │   ├── saju.js       # 사주 관련 API
│   │   └── user.js       # 사용자 관련 API
│   ├── controllers/
│   │   ├── paymentController.js
│   │   ├── sajuController.js
│   │   └── userController.js
│   ├── models/
│   │   ├── User.js
│   │   ├── Payment.js
│   │   └── SajuResult.js
│   └── services/
│       ├── portoneService.js   # 포트원 결제
│       ├── kakaoService.js     # 카카오 알림톡
│       └── pdfService.js       # PDF 생성
```

#### 🔧 기본 서버 코드 예시

```javascript
// backend/src/index.js
const express = require('express');
const cors = require('cors');
const dotenv = require('dotenv');

dotenv.config();

const app = express();

// 미들웨어 설정
app.use(cors({
  origin: 'http://localhost:5173', // 프론트엔드 주소
  credentials: true
}));
app.use(express.json());

// 라우트 연결
app.use('/api/payment', require('./routes/payment'));
app.use('/api/saju', require('./routes/saju'));
app.use('/api/user', require('./routes/user'));

// 서버 시작
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`서버가 포트 ${PORT}에서 실행 중입니다.`);
});
```

#### 📦 필요한 npm 패키지

```bash
npm init -y
npm install express cors dotenv mysql2 axios
npm install -D nodemon
```

---

### 2. MySQL DB 설계

#### 📊 테이블 구조

```sql
-- 사용자 테이블 (결제 정보 기반, 비회원)
CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  birth_date DATE NOT NULL,
  birth_time VARCHAR(10),           -- "모름" 또는 "02:30" 형태
  gender ENUM('male', 'female') NOT NULL,
  calendar_type ENUM('solar', 'lunar') DEFAULT 'solar',
  access_token VARCHAR(100) UNIQUE, -- 결과 페이지 접근용 고유 토큰
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  -- 같은 휴대폰+생년월일 조합으로 본인 확인
  UNIQUE KEY unique_user (phone, birth_date)
);

-- 결제 테이블
CREATE TABLE payments (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  merchant_uid VARCHAR(100) UNIQUE NOT NULL, -- 주문번호 (우리가 생성)
  imp_uid VARCHAR(100),                       -- 포트원 결제 고유번호
  amount INT NOT NULL,                        -- 결제 금액
  product_type ENUM('basic', 'pdf') NOT NULL, -- 기본 사주 or PDF 추가
  status ENUM('pending', 'paid', 'cancelled', 'refunded') DEFAULT 'pending',
  paid_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 사주 결과 테이블
CREATE TABLE saju_results (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  
  -- 사주 팔자 (API에서 받아온 원본 데이터)
  saju_data JSON,                    -- 만세력 API 응답 저장
  
  -- 해석된 결과 (텍스트)
  overall_fortune TEXT,              -- 총운
  wealth_fortune TEXT,               -- 재물운
  love_fortune TEXT,                 -- 애정운
  career_fortune TEXT,               -- 직장운
  health_fortune TEXT,               -- 건강운
  
  -- 점수
  overall_score INT,
  wealth_score INT,
  love_score INT,
  career_score INT,
  health_score INT,
  
  -- 오행 분석
  oheng_data JSON,                   -- { "목": 20, "화": 60, ... }
  
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 알림톡 발송 기록
CREATE TABLE notifications (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  type ENUM('result_link', 'delay_notice', 'pdf_complete') NOT NULL,
  phone VARCHAR(20) NOT NULL,
  status ENUM('pending', 'sent', 'failed') DEFAULT 'pending',
  sent_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 🔌 DB 연결 코드

```javascript
// backend/src/config/database.js
const mysql = require('mysql2/promise');

// 커넥션 풀 생성 (여러 요청을 효율적으로 처리)
const pool = mysql.createPool({
  host: process.env.DB_HOST || 'localhost',
  user: process.env.DB_USER || 'root',
  password: process.env.DB_PASSWORD || '',
  database: process.env.DB_NAME || 'saju_db',
  waitForConnections: true,
  connectionLimit: 10,
});

module.exports = pool;
```

#### 📝 사용 예시

```javascript
// 사용자 생성 예시
const db = require('../config/database');

async function createUser(userData) {
  const { name, phone, birthDate, birthTime, gender, calendarType } = userData;
  
  // 고유 접근 토큰 생성 (결과 페이지 URL에 사용)
  const accessToken = generateRandomToken(); // 랜덤 문자열 생성
  
  const [result] = await db.execute(
    `INSERT INTO users (name, phone, birth_date, birth_time, gender, calendar_type, access_token)
     VALUES (?, ?, ?, ?, ?, ?, ?)`,
    [name, phone, birthDate, birthTime, gender, calendarType, accessToken]
  );
  
  return { userId: result.insertId, accessToken };
}
```

---

### 3. 사주 API 연동

#### 🔮 만세력 API란?

만세력 API는 생년월일시를 입력하면 **사주팔자(四柱八字)**를 계산해주는 API입니다.

- **사주**: 년주, 월주, 일주, 시주 (4개의 기둥)
- **팔자**: 각 기둥당 천간+지지 = 8글자

#### 📡 API 연동 예시 (가상의 만세력 API)

```javascript
// backend/src/services/sajuApiService.js
const axios = require('axios');

/**
 * 만세력 API를 호출하여 사주팔자 데이터를 가져옵니다.
 * 
 * @param {Object} params - 사용자 생년월일시 정보
 * @returns {Object} 사주팔자 데이터
 */
async function getSajuData({ birthDate, birthTime, calendarType }) {
  try {
    // 실제 API 주소로 교체 필요
    const response = await axios.post('https://api.example.com/saju', {
      year: birthDate.split('-')[0],
      month: birthDate.split('-')[1],
      day: birthDate.split('-')[2],
      hour: birthTime || '12:00', // 모름인 경우 정오로 계산
      calendarType: calendarType, // 'solar' or 'lunar'
    }, {
      headers: {
        'Authorization': `Bearer ${process.env.SAJU_API_KEY}`
      }
    });
    
    return response.data;
    
    /* 응답 예시:
    {
      "yearPillar": { "heavenlyStem": "갑", "earthlyBranch": "자" },
      "monthPillar": { "heavenlyStem": "을", "earthlyBranch": "축" },
      "dayPillar": { "heavenlyStem": "병", "earthlyBranch": "인" },
      "timePillar": { "heavenlyStem": "정", "earthlyBranch": "묘" },
      "fiveElements": { "wood": 20, "fire": 60, "earth": 10, "metal": 5, "water": 5 },
      "mainElement": "fire",
      "fortune2026": { ... }
    }
    */
  } catch (error) {
    console.error('만세력 API 호출 실패:', error);
    throw error;
  }
}

module.exports = { getSajuData };
```

#### 🎯 사주 결과 생성 플로우

```javascript
// backend/src/controllers/sajuController.js

/**
 * 사주 분석 요청 처리
 * 1. 만세력 API로 사주 데이터 가져오기
 * 2. 운세 해석 텍스트 생성 (템플릿 또는 AI)
 * 3. DB에 저장
 * 4. 결과 반환
 */
async function analyzeSaju(req, res) {
  const { userId } = req.body;
  
  try {
    // 1. 사용자 정보 조회
    const user = await getUserById(userId);
    
    // 2. 만세력 API 호출
    const sajuData = await getSajuData({
      birthDate: user.birth_date,
      birthTime: user.birth_time,
      calendarType: user.calendar_type
    });
    
    // 3. 운세 해석 생성 (간단한 템플릿 방식)
    const interpretation = generateInterpretation(sajuData);
    
    // 4. DB에 저장
    await saveSajuResult(userId, sajuData, interpretation);
    
    // 5. 결과 반환
    res.json({
      success: true,
      result: {
        sajuData,
        interpretation,
        accessToken: user.access_token
      }
    });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
}
```

#### 💡 만세력 API 대안

1. **유료 API 서비스**
   - 만세력닷컴 API
   - 천문역법 API
   
2. **직접 계산 (라이브러리 사용)**
   - npm: `lunar-javascript` (음력/양력 변환)
   - 천간지지 계산 로직 직접 구현

3. **AI 활용**
   - OpenAI API로 사주 해석 텍스트 생성
   - 만세력 계산은 별도 + 해석만 AI

---

### 4. 포트원(아임포트) 결제 연동

#### 💳 결제 플로우 이해하기

```
[프론트엔드]                    [백엔드]                    [포트원/PG사]
     |                            |                            |
     |  1. 결제 요청              |                            |
     |--------------------------->|                            |
     |                            |  2. merchant_uid 생성       |
     |  3. merchant_uid 반환      |                            |
     |<---------------------------|                            |
     |                            |                            |
     |  4. 포트원 SDK로 결제창 호출 |                            |
     |-------------------------------------------------------->|
     |                            |                            |
     |  5. 사용자가 결제 진행      |                            |
     |                            |                            |
     |  6. 결제 완료 (imp_uid 반환)|                            |
     |<--------------------------------------------------------|
     |                            |                            |
     |  7. 결제 검증 요청          |                            |
     |--------------------------->|                            |
     |                            |  8. 포트원 API로 검증        |
     |                            |--------------------------->|
     |                            |  9. 검증 결과               |
     |                            |<---------------------------|
     |  10. 최종 결과 반환         |                            |
     |<---------------------------|                            |
```

#### 📱 프론트엔드 코드 (결제 요청)

```jsx
// src/components/Payment.jsx

/**
 * 포트원 결제 요청 함수
 * 포트원 SDK를 사용하여 결제창을 띄웁니다.
 */
const handlePayment = async () => {
  // 1. 백엔드에서 주문번호 받아오기
  const { merchantUid, amount } = await fetch('/api/payment/prepare', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ userId: userInfo.id, productType: 'basic' })
  }).then(res => res.json());
  
  // 2. 포트원 SDK 불러오기 (index.html에 스크립트 추가 필요)
  const { IMP } = window;
  IMP.init('imp00000000'); // 포트원 가맹점 식별코드
  
  // 3. 결제 요청
  IMP.request_pay({
    pg: 'nice',                    // PG사 (나이스페이먼츠)
    pay_method: paymentMethod,      // 'card', 'kakaopay', 'naverpay' 등
    merchant_uid: merchantUid,      // 주문번호
    name: '2026년 사주 운세 분석',   // 상품명
    amount: amount,                 // 결제 금액
    buyer_name: userInfo.name,
    buyer_tel: userInfo.phone,
  }, async (response) => {
    // 4. 결제 완료 콜백
    if (response.success) {
      // 5. 백엔드에 결제 검증 요청
      const verifyResult = await fetch('/api/payment/verify', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          imp_uid: response.imp_uid,
          merchant_uid: response.merchant_uid
        })
      }).then(res => res.json());
      
      if (verifyResult.success) {
        // 결제 성공! 분석 시작
        startAnalysis();
      } else {
        alert('결제 검증 실패');
      }
    } else {
      alert(`결제 실패: ${response.error_msg}`);
    }
  });
};
```

#### 🖥️ 백엔드 코드 (결제 검증)

```javascript
// backend/src/controllers/paymentController.js
const axios = require('axios');

/**
 * 결제 준비 - 주문번호 생성
 */
async function preparePayment(req, res) {
  const { userId, productType } = req.body;
  
  // 고유한 주문번호 생성
  const merchantUid = `order_${Date.now()}_${userId}`;
  const amount = productType === 'basic' ? 38800 : 3900;
  
  // DB에 결제 대기 상태로 저장
  await db.execute(
    `INSERT INTO payments (user_id, merchant_uid, amount, product_type, status)
     VALUES (?, ?, ?, ?, 'pending')`,
    [userId, merchantUid, amount, productType]
  );
  
  res.json({ merchantUid, amount });
}

/**
 * 결제 검증 - 포트원 API로 실제 결제 확인
 */
async function verifyPayment(req, res) {
  const { imp_uid, merchant_uid } = req.body;
  
  try {
    // 1. 포트원 액세스 토큰 발급
    const tokenResponse = await axios.post(
      'https://api.iamport.kr/users/getToken',
      {
        imp_key: process.env.PORTONE_API_KEY,
        imp_secret: process.env.PORTONE_API_SECRET
      }
    );
    const accessToken = tokenResponse.data.response.access_token;
    
    // 2. 포트원에서 결제 정보 조회
    const paymentResponse = await axios.get(
      `https://api.iamport.kr/payments/${imp_uid}`,
      { headers: { 'Authorization': accessToken } }
    );
    const paymentData = paymentResponse.data.response;
    
    // 3. DB에서 우리가 저장한 결제 정보 조회
    const [rows] = await db.execute(
      'SELECT * FROM payments WHERE merchant_uid = ?',
      [merchant_uid]
    );
    const ourPayment = rows[0];
    
    // 4. 금액 일치 여부 검증 (위변조 방지)
    if (paymentData.amount === ourPayment.amount) {
      // 결제 성공! DB 업데이트
      await db.execute(
        `UPDATE payments SET status = 'paid', imp_uid = ?, paid_at = NOW()
         WHERE merchant_uid = ?`,
        [imp_uid, merchant_uid]
      );
      
      res.json({ success: true, message: '결제가 완료되었습니다.' });
    } else {
      // 금액 불일치 - 위변조 의심
      res.json({ success: false, message: '결제 금액이 일치하지 않습니다.' });
    }
  } catch (error) {
    console.error('결제 검증 실패:', error);
    res.status(500).json({ success: false, error: error.message });
  }
}

module.exports = { preparePayment, verifyPayment };
```

#### 🔑 포트원 설정 방법

1. [포트원 관리자 콘솔](https://admin.portone.io/) 가입
2. 테스트 모드로 시작 (사업자등록 없이 가능)
3. PG사 설정 (나이스페이먼츠, KG이니시스 등)
4. 가맹점 식별코드, API Key, API Secret 발급

---

## Phase 3: 알림 및 PDF

### 1. 카카오 알림톡 연동

#### 📱 알림톡이란?

- 카카오톡 채널을 통해 보내는 **템플릿 기반 메시지**
- 광고성 메시지와 달리 **수신 동의 없이** 발송 가능
- 단, 미리 등록된 **템플릿만** 사용 가능

#### 🔧 연동 방법 (비즈엠 API 예시)

```javascript
// backend/src/services/kakaoService.js
const axios = require('axios');

/**
 * 카카오 알림톡 발송 서비스
 * 비즈엠, 알리고 등의 중계사 API를 사용
 */
class KakaoAlimtalkService {
  constructor() {
    this.apiUrl = 'https://kakaoapi.aligo.in/akv10/alimtalk/send/';
    this.apiKey = process.env.ALIGO_API_KEY;
    this.userId = process.env.ALIGO_USER_ID;
    this.senderKey = process.env.KAKAO_SENDER_KEY;
  }
  
  /**
   * 결과 조회 링크 알림톡 발송
   * 
   * @param {string} phone - 수신자 전화번호
   * @param {string} name - 수신자 이름
   * @param {string} resultUrl - 결과 조회 URL
   */
  async sendResultLink(phone, name, resultUrl) {
    const templateCode = 'SAJU_RESULT_001'; // 미리 등록한 템플릿 코드
    
    // 템플릿 예시:
    // "#{이름}님의 2026년 사주 분석이 완료되었습니다.
    //  아래 버튼을 눌러 결과를 확인하세요."
    
    const response = await axios.post(this.apiUrl, {
      apikey: this.apiKey,
      userid: this.userId,
      senderkey: this.senderKey,
      tpl_code: templateCode,
      sender: '01012345678',     // 발신 번호
      receiver_1: phone,         // 수신 번호
      subject_1: '사주 분석 완료',
      message_1: `${name}님의 2026년 사주 분석이 완료되었습니다.\n아래 버튼을 눌러 결과를 확인하세요.`,
      button_1: JSON.stringify({
        button: [{
          name: '결과 확인하기',
          linkType: 'WL',        // 웹 링크
          linkTypeName: '웹링크',
          linkM: resultUrl,      // 모바일 URL
          linkP: resultUrl       // PC URL
        }]
      })
    });
    
    return response.data;
  }
  
  /**
   * 지연 안내 알림톡 발송
   */
  async sendDelayNotice(phone, name, estimatedTime) {
    // 템플릿: "#{이름}님, 현재 분석 요청이 많아 약 #{시간}분 후 완료 예정입니다."
    // ... 위와 유사한 방식
  }
}

module.exports = new KakaoAlimtalkService();
```

#### 📋 알림톡 설정 절차

1. **카카오 비즈니스 채널 생성**
   - [카카오 비즈니스](https://business.kakao.com/) 접속
   - 카카오톡 채널 개설

2. **알림톡 중계사 가입**
   - 알리고, 비즈엠, NHN 클라우드 등
   - 사업자등록 필요

3. **템플릿 등록 및 검수**
   - 발송할 메시지 템플릿 작성
   - 카카오 검수 승인 (1-3일 소요)

4. **API 연동**
   - 발급받은 키로 API 호출

---

### 2. PDF 생성 기능

#### 📄 구현 방식 2가지

**방식 1: 프론트엔드에서 생성 (html2canvas + jspdf)**
- 장점: 서버 부하 없음, 빠른 구현
- 단점: 품질 제한, 브라우저 의존

**방식 2: 백엔드에서 생성 (Puppeteer)**
- 장점: 고품질, 일관된 결과
- 단점: 서버 리소스 사용

#### 🎨 방식 1: 프론트엔드 PDF 생성

```jsx
// src/utils/pdfGenerator.js
import html2canvas from 'html2canvas';
import jsPDF from 'jspdf';

/**
 * 결과 페이지를 PDF로 변환하여 다운로드
 * 
 * @param {string} elementId - 캡처할 HTML 요소의 ID
 * @param {string} fileName - 저장할 파일명
 */
export async function generatePDF(elementId, fileName) {
  const element = document.getElementById(elementId);
  
  // 1. HTML을 캔버스로 변환
  const canvas = await html2canvas(element, {
    scale: 2,                    // 고해상도
    useCORS: true,               // 외부 이미지 허용
    backgroundColor: '#0f172a', // 배경색
  });
  
  // 2. 캔버스를 이미지로 변환
  const imgData = canvas.toDataURL('image/png');
  
  // 3. PDF 생성
  const pdf = new jsPDF({
    orientation: 'portrait',
    unit: 'mm',
    format: 'a4'
  });
  
  // 4. 이미지를 PDF에 추가
  const imgWidth = 210;  // A4 가로
  const imgHeight = (canvas.height * imgWidth) / canvas.width;
  
  pdf.addImage(imgData, 'PNG', 0, 0, imgWidth, imgHeight);
  
  // 5. 다운로드
  pdf.save(`${fileName}.pdf`);
}
```

#### 사용 예시

```jsx
// 결과 페이지에서
import { generatePDF } from '../utils/pdfGenerator';

const handleDownloadPDF = async () => {
  // PDF 결제 확인 후...
  await generatePDF('saju-result-content', `${userInfo.name}_사주분석`);
};

// 결과 컨텐츠에 ID 부여
<div id="saju-result-content">
  {/* 결과 내용 */}
</div>
```

#### 🖥️ 방식 2: 백엔드 PDF 생성 (Puppeteer)

```javascript
// backend/src/services/pdfService.js
const puppeteer = require('puppeteer');
const path = require('path');
const fs = require('fs');

/**
 * Puppeteer를 사용한 고품질 PDF 생성
 * 헤드리스 브라우저로 페이지를 렌더링하고 PDF로 저장
 */
class PDFService {
  /**
   * 사주 결과 PDF 생성
   * 
   * @param {string} accessToken - 사용자 접근 토큰
   * @returns {string} 생성된 PDF 파일 경로
   */
  async generateSajuPDF(accessToken) {
    const browser = await puppeteer.launch({
      headless: 'new',
      args: ['--no-sandbox']
    });
    
    try {
      const page = await browser.newPage();
      
      // 결과 페이지 접속 (PDF 전용 레이아웃)
      await page.goto(
        `${process.env.FRONTEND_URL}/result/${accessToken}?mode=pdf`,
        { waitUntil: 'networkidle0' }
      );
      
      // 페이지 사이즈 설정
      await page.setViewport({ width: 595, height: 842 }); // A4 사이즈
      
      // PDF 생성
      const pdfPath = path.join(__dirname, `../../temp/${accessToken}.pdf`);
      await page.pdf({
        path: pdfPath,
        format: 'A4',
        printBackground: true,  // 배경색 포함
        margin: { top: '10mm', bottom: '10mm', left: '10mm', right: '10mm' }
      });
      
      return pdfPath;
    } finally {
      await browser.close();
    }
  }
  
  /**
   * PDF 파일 S3 업로드 (선택사항)
   */
  async uploadToS3(filePath) {
    // AWS S3 업로드 로직
    // ...
  }
}

module.exports = new PDFService();
```

#### 📦 필요한 패키지

```bash
# 프론트엔드
npm install html2canvas jspdf

# 백엔드 (Puppeteer 사용 시)
npm install puppeteer
```

---

## Phase 4: 관리자 페이지

### 📊 관리자 페이지 기능

```jsx
// admin/src/pages/Dashboard.jsx
/**
 * 관리자 대시보드
 * - 일별 매출 현황
 * - 총 결제 건수
 * - 환불 요청 현황
 */

// admin/src/pages/Payments.jsx
/**
 * 결제 내역 관리
 * - 결제 목록 조회 (검색, 필터링)
 * - 결제 상세 정보
 * - 환불 처리
 */

// admin/src/pages/Contents.jsx
/**
 * 콘텐츠 관리
 * - 운세 텍스트 템플릿 수정
 * - 오행별 해석 문구 관리
 */
```

### 🔐 관리자 인증

```javascript
// backend/src/middleware/adminAuth.js
const jwt = require('jsonwebtoken');

/**
 * 관리자 인증 미들웨어
 * JWT 토큰으로 관리자 권한 확인
 */
function adminAuth(req, res, next) {
  const token = req.headers.authorization?.split(' ')[1];
  
  if (!token) {
    return res.status(401).json({ error: '인증이 필요합니다.' });
  }
  
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    
    if (decoded.role !== 'admin') {
      return res.status(403).json({ error: '관리자 권한이 없습니다.' });
    }
    
    req.admin = decoded;
    next();
  } catch (error) {
    res.status(401).json({ error: '유효하지 않은 토큰입니다.' });
  }
}

module.exports = adminAuth;
```

### 💰 환불 처리 API

```javascript
// backend/src/controllers/adminController.js

/**
 * 결제 환불 처리
 * 포트원 API를 통해 실제 환불 진행
 */
async function refundPayment(req, res) {
  const { paymentId, reason } = req.body;
  
  try {
    // 1. 결제 정보 조회
    const payment = await getPaymentById(paymentId);
    
    // 2. 포트원 환불 API 호출
    const accessToken = await getPortoneToken();
    
    const response = await axios.post(
      'https://api.iamport.kr/payments/cancel',
      {
        imp_uid: payment.imp_uid,
        reason: reason,
        // amount: 부분환불 시 금액 지정
      },
      { headers: { 'Authorization': accessToken } }
    );
    
    if (response.data.code === 0) {
      // 3. DB 상태 업데이트
      await db.execute(
        `UPDATE payments SET status = 'refunded' WHERE id = ?`,
        [paymentId]
      );
      
      res.json({ success: true, message: '환불이 완료되었습니다.' });
    } else {
      res.json({ success: false, message: response.data.message });
    }
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
}
```

---

## 전체 시스템 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              사용자 플로우                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    [랜딩 페이지]
         │
         ▼
    [정보 입력] ─────────────────────────────────────────┐
    - 이름, 생년월일, 시간, 성별, 휴대폰                    │
         │                                               │
         ▼                                               │
    [결제 페이지]                                         │
    - 포트원 SDK로 결제창 호출                             │
         │                                               │
         ▼                                               │
    ┌─────────────────┐                                  │
    │   결제 진행      │                                  │
    │  (PG사 처리)     │                                  │
    └─────────────────┘                                  │
         │                                               │
         ▼                                               │
    [백엔드: 결제 검증]                                   │
    - 포트원 API로 금액 확인                               │
    - DB에 결제 정보 저장                                  │
         │                                               │
         ▼                                               │
    [사주 분석]                                           │
    - 만세력 API 호출                                     │
    - 운세 해석 생성                                      │
    - 결과 DB 저장                                        │
         │                                               │
         ├──────────────────────────┐                    │
         ▼                          ▼                    │
    [결과 페이지]            [카카오 알림톡 발송]           │
    - 즉시 결과 표시          - 결과 조회 링크 포함         │
         │                          │                    │
         │                          ▼                    │
         │                    [사용자: 카톡 수신]          │
         │                          │                    │
         │                          ▼                    │
         │                    [링크 클릭 → 간편인증]       │
         │                    - 생년월일 + 휴대폰          │
         │                          │                    │
         │◀─────────────────────────┘                    │
         │                                               │
         ▼                                               │
    [PDF 추가 구매?]                                      │
         │                                               │
         ├── No ──▶ [웹에서 계속 보기]                    │
         │                                               │
         └── Yes ─▶ [PDF 결제 (3,900원)]                 │
                          │                              │
                          ▼                              │
                    [PDF 생성 & 다운로드]                  │
                                                         │
─────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                              시스템 구성도                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
    │   Frontend   │      │   Backend    │      │   Database   │
    │   (React)    │◀────▶│  (Express)   │◀────▶│   (MySQL)    │
    └──────────────┘      └──────────────┘      └──────────────┘
           │                     │
           │                     │
           ▼                     ▼
    ┌──────────────┐      ┌──────────────┐
    │   포트원      │      │  외부 API    │
    │  (결제 SDK)   │      │              │
    └──────────────┘      ├──────────────┤
                          │ - 만세력 API  │
                          │ - 카카오 알림톡│
                          │ - AWS S3     │
                          └──────────────┘
```

---

## 📝 개발 순서 권장

### Step 1: 백엔드 기초 (1-2주)
1. Express 서버 세팅
2. MySQL 연결 및 테이블 생성
3. 기본 CRUD API 구현

### Step 2: 결제 연동 (1주)
1. 포트원 테스트 모드 설정
2. 결제 요청/검증 API 구현
3. 프론트엔드 연동

### Step 3: 사주 API (1주)
1. 만세력 API 선정 및 연동
2. 결과 저장 로직 구현
3. 프론트엔드 결과 표시 연동

### Step 4: 알림톡 (3-5일)
1. 카카오 비즈니스 채널 개설
2. 알림톡 중계사 가입
3. 템플릿 등록 및 API 연동

### Step 5: PDF (3-5일)
1. html2canvas + jspdf 설치
2. PDF 레이아웃 최적화
3. 다운로드 기능 구현

### Step 6: 관리자 페이지 (1주)
1. 별도 React 프로젝트 또는 라우트 추가
2. 결제 내역 조회/환불 기능
3. 콘텐츠 관리 기능

---

## 🔗 유용한 링크

- [포트원 개발자 문서](https://developers.portone.io/)
- [카카오 비즈니스](https://business.kakao.com/)
- [알리고 알림톡 API](https://smartsms.aligo.in/admin/api/kakao.php)
- [jsPDF 문서](https://github.com/parallax/jsPDF)
- [html2canvas 문서](https://html2canvas.hertzen.com/)

---

> 💡 **팁**: 처음에는 복잡해 보이지만, 각 단계를 하나씩 진행하면 충분히 구현 가능합니다!
> 막히는 부분이 있으면 언제든 질문해주세요.

